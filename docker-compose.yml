services:
  comments-api:
    build: .
    ports:
      - "8081:8080"
    environment:
      - ASPNETCORE_URLS=http://0.0.0.0:8080
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - comments_data:/data
    restart: unless-stopped

volumes:
  comments_data:
